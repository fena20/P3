# وضعیت دقیق دیتاست در حال استفاده

## 📊 پاسخ مستقیم

**در حال حاضر روی دیتای سنتتیک (Synthetic Data) کار می‌کنم، نه دیتاست واقعی AMPds2.**

---

## 🔍 جزئیات دقیق دیتاست فعلی

### نوع دیتاست:
**Synthetic/Mock Data** - دیتای تولید شده توسط کد

### مشخصات:
- **تعداد نمونه**: 525,600 نمونه
- **مدت زمان**: 365 روز (یک سال کامل)
- **رزولوشن**: 1 دقیقه (مثل AMPds2)
- **تاریخ شروع**: 2012-04-01 00:00:00
- **تاریخ پایان**: 2013-03-31 23:59:00

### ستون‌ها:
1. `timestamp`: زمان (DateTime)
2. `WHE`: مصرف انرژی آبگرمکن (Water Heater Energy)
3. `HPE`: مصرف انرژی پمپ حرارتی (Heat Pump Energy)
4. `FRE`: مصرف انرژی یخچال (Fridge Energy)
5. `Outdoor_Temp`: دمای بیرون (°C)
6. `Solar_Rad`: تابش خورشید (W/m²)
7. `Price`: قیمت برق ($/kWh)

---

## 🔬 نحوه تولید دیتای سنتتیک

دیتای سنتتیک در فایل `data_loader.py` تولید می‌شود:

### 1. دمای بیرون (Outdoor_Temp):
```python
# الگوی سینوسی فصلی + روزانه + نویز
seasonal_temp = 15 + 10 * sin(2π * day/365)  # تغییر فصلی
daily_temp = 5 * sin(2π * hour/24)            # تغییر روزانه
outdoor_temp = seasonal_temp + daily_temp + noise
```

**نتیجه**: دمای واقعی‌گرا با تغییرات فصلی و روزانه

### 2. تابش خورشید (Solar_Rad):
```python
# صفر در شب، پیک در ظهر
solar_rad = 800 * sin(π * (hour - 6) / 12)  # 6 صبح تا 6 عصر
solar_rad[hour < 6 or hour > 18] = 0         # صفر در شب
```

**نتیجه**: الگوی واقعی‌گرای تابش خورشید

### 3. قیمت برق (Price):
```python
base_price = 0.10  # $/kWh
# Peak pricing: 17:00-20:00
price = 0.10 * (1.5 if 17 <= hour < 20 else 1.0)
```

**نتیجه**: Time-of-Use pricing (قیمت بالاتر در ساعات پیک)

### 4. مصرف انرژی:
- **WHE**: توزیع نمایی با spikes تصادفی
- **HPE**: توزیع نرمال (2.5 kW اگر دما < 18°C)
- **FRE**: تقریباً ثابت (0.15 kW)

---

## ❌ دیتاست واقعی AMPds2

### وضعیت فعلی:
**دیتاست واقعی AMPds2 در سیستم وجود ندارد**

### بررسی انجام شده:
```bash
# بررسی پوشه data/
ls -la data/  # نتیجه: No data directory found

# بررسی فایل‌های CSV
find . -name "*.csv"  # فقط فایل‌های نتایج (tables, sensitivity)
```

### نتیجه:
- ❌ هیچ فایل دیتاست واقعی در سیستم نیست
- ✅ کد آماده است برای استفاده از دیتاست واقعی
- ⚠️ فعلاً از دیتای سنتتیک استفاده می‌شود

---

## 🔄 نحوه تشخیص در کد

### در `data_loader.py`:
```python
def load_ampds2_data(filepath=None):
    if filepath and pd.io.common.file_exists(filepath):
        df = pd.read_csv(filepath, parse_dates=['timestamp'])
        # استفاده از دیتاست واقعی
    else:
        print("No file provided or file not found. Generating synthetic AMPds2-like data...")
        df = generate_synthetic_ampds2_data()
        # استفاده از دیتای سنتتیک ← این حالت فعلی است
```

### پیام فعلی:
```
"No file provided or file not found. Generating synthetic AMPds2-like data..."
```

این پیام نشان می‌دهد که از دیتای سنتتیک استفاده می‌شود.

---

## 📈 مقایسه: سنتتیک vs واقعی

| ویژگی | دیتای سنتتیک (فعلی) | دیتاست واقعی AMPds2 |
|-------|---------------------|---------------------|
| **منبع** | تولید شده در کد | دانلود از ampds.org |
| **تعداد نمونه** | 525,600 (365 روز) | متغیر (بسته به دیتاست) |
| **رزولوشن** | 1 دقیقه ✅ | 1 دقیقه ✅ |
| **ستون‌ها** | WHE, HPE, FRE, Temp, Solar, Price ✅ | مشابه ✅ |
| **الگوها** | سینوسی + تصادفی | واقعی از ساختمان |
| **قابل تکرار** | بله (با seed) | بله |
| **برای تست** | مناسب ✅ | مناسب ✅ |
| **برای مقاله** | ⚠️ باید واقعی باشد | مناسب ✅ |

---

## ✅ چک‌لیست وضعیت

- [x] دیتای سنتتیک تولید می‌شود
- [x] 525,600 نمونه (365 روز × 24 ساعت × 60 دقیقه)
- [x] رزولوشن 1 دقیقه (مثل AMPds2)
- [x] ستون‌های مورد نیاز موجود است
- [x] الگوهای واقعی‌گرا (فصلی، روزانه، TOU pricing)
- [ ] دیتاست واقعی AMPds2 در سیستم نیست
- [x] کد آماده است برای استفاده از دیتاست واقعی

---

## 🎯 برای استفاده از دیتاست واقعی

### مرحله 1: دانلود
1. ثبت‌نام در: https://ampds.org/
2. دانلود دیتاست AMPds2
3. استخراج فایل CSV

### مرحله 2: قرار دادن در سیستم
```bash
mkdir -p ./data/ampds2/
# قرار دادن فایل CSV در این پوشه
cp your_ampds2_data.csv ./data/ampds2/
```

### مرحله 3: استفاده در کد
```python
from smarthome_env import SmartHomeEnv

# استفاده از دیتاست واقعی
env = SmartHomeEnv(data_path="./data/ampds2/your_ampds2_data.csv")
```

### مرحله 4: بررسی
اگر دیتاست واقعی بارگذاری شود، پیام زیر نمایش داده نمی‌شود:
```
"No file provided or file not found. Generating synthetic AMPds2-like data..."
```

---

## 📝 خلاصه

### وضعیت فعلی:
✅ **دیتای سنتتیک** در حال استفاده است
- 525,600 نمونه
- 365 روز (2012-04-01 تا 2013-03-31)
- رزولوشن 1 دقیقه
- الگوهای واقعی‌گرا

### وضعیت دیتاست واقعی:
❌ **دیتاست واقعی AMPds2 موجود نیست**
- کد آماده است برای استفاده
- نیاز به دانلود از ampds.org

### برای مقاله:
⚠️ **باید از دیتاست واقعی استفاده کنید**
- دیتای سنتتیک برای تست و توسعه مناسب است
- برای مقاله Applied Energy باید دیتاست واقعی باشد

---

## 🔍 نحوه بررسی در آینده

برای بررسی اینکه از دیتاست واقعی استفاده می‌شود یا سنتتیک:

```python
from smarthome_env import SmartHomeEnv

env = SmartHomeEnv()
# اگر پیام زیر نمایش داده نشد، از دیتاست واقعی استفاده می‌شود:
# "No file provided or file not found. Generating synthetic AMPds2-like data..."
```

یا بررسی مستقیم:
```python
# بررسی منبع داده
if hasattr(env.data, 'source'):
    print(f"Data source: {env.data.source}")
else:
    # اگر source مشخص نباشد، احتمالاً سنتتیک است
    print("Likely synthetic data")
```

---

**نتیجه‌گیری**: در حال حاضر روی **دیتای سنتتیک** کار می‌کنم که الگوهای AMPds2 را شبیه‌سازی می‌کند. دیتاست واقعی در سیستم وجود ندارد اما کد آماده است برای استفاده از آن.
